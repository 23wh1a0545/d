7
package.json
{
  "name": "getting-started-app",
  "version": "1.0.0",
  "description": "Simple Node.js app in Docker",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
Dockerfile
# Use small Node base image
FROM node:18-alpine

# Install required build tools
RUN apk add --no-cache python3 g++ make

# Create app directory
WORKDIR /app

# Copy package files first (for caching)
COPY package*.json ./

# Install only production dependencies
RUN npm install --production

# Copy source code
COPY . .

# Expose port
EXPOSE 3000

# Start the app
CMD ["node", "src/index.js"]
index.js
const express = require('express');

const app = express();
const PORT = 3000;

app.get("/", (req, res) => {
    res.send("Hello from dockerized Node.js app!");
});

app.listen(PORT, () => {
    console.log(`Server is running on http://localhost:${PORT}`);
});
cd path/to/app-545
docker build -t getting-started .
docker run -dp 3000:3000 getting-started
docker ps
localhost:3000

2
git init
git add : echo "first" > README.md ,git status, git commit -m "initial commit", git config --global user.name "xyz",git config --list , git branch feature, git checkout feature, git merge feature, 
remote repo; git remote add origin <github-id>, remote -v, git clone <github_id> , git pull, git push -u origin main
6
docker run -it ubuntu
#pwd
#ls
#apt update
apt install curl
echo "hell from docker ">hello.txt
cat hello.txt
exit
docker ps
docker stop <>
docker rm <>
10
HelloController.java
package com.example.devopslab;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class DevOpsLabController {

    @GetMapping("/hello")
    public String sayHello() {
        return "Hello from DevOps Lab!";
    }
}
KarateTestRunner.java
package com.example.devopslab
import com.intuit.karate.junit5.karate;
public class karateTestRunner{
@karate.Test
Karate testAll(){
return karate.run("classpath:features/sample.feature");
}
}
mvn clean install 
mvn spring boot: run
